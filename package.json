{
  "name": "ver-sacrum",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Ver Sacrum — dimora nel centro storico di Ascoli Piceno",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node scripts/images.js && vite --host 127.0.0.1",
    "build": "node scripts/images.js && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "npm run test:seo && playwright test",
    "test:seo": "node --test tests/seo.unit.js",
    "format": "prettier --write src/index.html src/styles.css src/*.js scripts/*.js tests/*.js *.config.js README.md TODO.md package.json .github/workflows/deploy.yml",
    "format:check": "prettier --check src/index.html src/styles.css src/*.js scripts/*.js tests/*.js *.config.js README.md TODO.md package.json .github/workflows/deploy.yml"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.8",
    "sharp": "^0.35.4",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "@fontsource/cormorant-garamond": "^5.3.0",
    "@fontsource/manrope": "^5.3.0"
  }
}
